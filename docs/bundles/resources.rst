The ``resources.xml`` file
==========================

The ``resources.xml`` file contains the index of all files contained in a
:ref:`talk bundle <talk bundle>` and the different relationships between them.
To be successfully recognized as such, it as to be placed at the root of the
respective talk bundle.
